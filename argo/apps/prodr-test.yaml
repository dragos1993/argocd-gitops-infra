apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prodr-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dragos1993/argocd-test.git
    targetRevision: main
    path: charts/nginx-app
    helm:
      valueFiles:
        - apps/prodr/test/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prodr-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true