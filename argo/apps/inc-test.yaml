apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: inc-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dragos1993/argocd-test.git
    targetRevision: main
    path: charts/nginx-app
    helm:
      valueFiles:
        - apps/inc/test/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: inc-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true